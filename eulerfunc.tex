ƒл€ натурального $n$ значение функции Ёйлера\index{‘ункци€ Ёйлера} $\phi(n)$ равно числу взаимно простых с $n$ чисел среди $\left\{1,2,\dots ,n-1\right\}$. Ќапример, $\phi(5)$=4, поскольку среди чисел $\left\{1,2,3,4\right\}$ все взаимно просты с $5$, $\phi(6)=2$, поскольку среди чисел $\left\{1,2,3,4,5\right\}$ только числа $1$ и $5$ взаимно просты с $6$.

ѕусть $n=p^{\alpha_1}_1\ldots p^{\alpha_k}_k$~-- каноническое разложение числа $n$, то есть разложение на простые множители, где $p_1,\ldots,p_k$~-- все простые попарно различные делители числа $n$, а $\alpha_1,\alpha_2,\ldots,\alpha_k$ отличные от нул€ положительные целые числа.
¬ общем случае верна формула:
\begin{equation}\label{eq:euler}
	\phi(n)=n\left(1-\frac{1}{p_1}\right)\dots \left(1-\frac{1}{p_k}\right)
\end{equation}
 ак следствие формулы получаем: если $n$ простое, то $\phi(n)=n-1$; если $p$ и $q$ простые числа, то $\phi(p\cdot q)=(p-1)(q-1)$.

\subsection*{”пражнени€}
\begin{enumerate}
	\item ƒокажите, что из $(n,m)=1$ следует $\phi(n\cdot m)=\phi(n)\cdot\phi(m)$.
		\item ƒокажите, что из простоты числа $p$ следует $\phi(p^n)=p^{n-1}\left(p-1\right)$.
			\item ƒокажите формулу~\ref{eq:euler}.
\end{enumerate}
